interface:
  display_name: "台本画像一括生成"
  short_description: "台本Markdownから各ページ画像を一括生成し、manifest付きで保存する"
  default_prompt: "Use $tiktok-fit-slide-image-generator to run /image with Antigravity image generation (nano banana, model=pro) and save slide images from prompts."
policy:
  allow_implicit_invocation: true
