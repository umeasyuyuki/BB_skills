interface:
  display_name: "Notion保存パブリッシャー"
  short_description: "投稿ドキュメント（調査/チェック/台本）をNotionへ自動保存する"
  default_prompt: "Use $tiktok-fit-notion-publisher to run /notion-target then /notion-publish (or package-create/package-save-notion) and return Japanese Markdown output."
policy:
  allow_implicit_invocation: true
